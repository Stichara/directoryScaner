#                                               Directory Scanner

## ќписание 
“ребуетс€ написать программу, котора€ будет сканировать заданную директорию, и копировать файлы, удовлетвор€ющие определенному условию в выходную директорию. ѕри этом сканер должен поддерживать следующий набор опций комнад: 
- inputDir - сканируема€ директори€ 
- outputDir - выходна€ директори€ 
- mask - маска дл€ файлов, которые должны копироватьс€ сканером 
- waitInterval - интервал, с которым сканер будет провер€ть заданную директорию 
- includeSubfolders - включать или не включать обработку поддиректорий (если включать, то сканер должен сохран€ть структуру поддиректорий при копировании в выходную директорию) 
- autoDelete - удал€ть или не удал€ть файлы после копировани€ в сканируемой директории ѕрограмма должна поддерживать многопоточность и работать под командной строкой приблизительно со следующим синтаксисом:


`c:>java my.app.DirectoryScannerConsole`

`scan Цinput УC:/test/inФ Цoutput УC:/test/outФ Цmask У*test*.xmlФ ЦwaitInterval 60000 Ц includeSubfolders false ЦautoDelete true`

`scan Цinput УC:/test1/inФ Цoutput УC:/test1/outФ Цmask У*test1*.xmlФ ЦwaitInterval 90000 Ц includeSubfolders true ЦautoDelete false`

 `Е`

`exit`

`c:>`


√де после запуска выводитс€ приглашение дл€ ввода команд. ¬ нашем случае мы имеем две команды: scan и exit. √де scan запускает сканер в отдельном потоке с заданными параметрами. » exit корректно останавливает работу всех запущенных сканеров и завершает работу программы.  аждый сканер должен писать лог информацию о процессе сканировани€ в отдельный файл. ƒл€ лог файлов должна поддерживатьс€ интернационализаци€ (€зык может задаватьс€ через Цlocale= ключ в командной строке запуска апликации).

## “ребовани€ к реализации

„истый и целесообразно документированый код
»спользование unit тестов (jUnit или другой framework).
»спользовать log4j дл€ логировани€.

## ѕриветствуетс€ 
”лучшени€ в функциональность сканера (например, добавление поддержки команды дл€ остановки выбранного сканера).