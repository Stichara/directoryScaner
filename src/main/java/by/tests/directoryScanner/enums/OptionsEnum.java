package by.tests.directoryScanner.enums;


public enum OptionsEnum {

    //options for scanner
    inputDir,
    outputDir,
    mask,
    waitInterval,
    includeSubfolders,
    autoDelete,

    //options for management
    idScan,
    locale
}
